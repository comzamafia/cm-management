{
  "framework": "nextjs",
  "buildCommand": "prisma generate && (prisma migrate resolve --applied 0_init || true) && prisma migrate deploy && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/run-all",
      "schedule": "0 23 * * *"
    }
  ]
}
